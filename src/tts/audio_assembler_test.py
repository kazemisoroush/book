"""Tests for AudioAssembler — audio post-processing: silence, stitching, ambient, sound effects.

Note: All tests were deleted because they each required 3-5 mocks/patches,
violating Rule 1 (at most 1 mock per test). The AudioAssembler design needs
refactoring to enable meaningful testing with fewer mocks.
"""
